package org.mentionbattle.configuration

class Configuration (port : Int, snaPort : Int, snaUrl: String, snaKey : String) {
    val snaPort = snaPort
    val snaUrl = snaUrl
    val snaKey = snaKey

    val port = port
}
